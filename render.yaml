services:
  - type: worker
    name: xoso-ai-bot
    runtime: python

    buildCommand: pip install -r requirements.txt
    startCommand: python main.py

    envVars:
      - key: BOT_TOKEN
        sync: false

      - key: CHAT_ID
        sync: false
